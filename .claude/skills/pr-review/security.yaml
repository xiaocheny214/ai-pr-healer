name: 安全审查
rules:
  - id: SEC-001
    check: "SQL 查询必须使用参数化，禁止字符串拼接"
    severity: critical
  - id: SEC-002
    check: "禁止使用 eval()、exec() 等动态执行函数"
    severity: high
  - id: SEC-003
    check: "API 端点必须有鉴权中间件"
    severity: high
